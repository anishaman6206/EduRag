# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Pydantic + settings
pydantic==2.9.2
pydantic-settings==2.5.2

# HTTP clients
httpx==0.27.2
aiohttp==3.10.10

# OpenAI — used for BOTH text completions (gpt-4o-mini) and embeddings
# (text-embedding-3-small). Single SDK, single API key, one billing line.
openai==1.54.3

# Pinecone vector DB
pinecone==5.0.1
pinecone-text==0.8.0   # BM25 sparse encoder

# Supabase
# Note: pin only the top-level SDK; postgrest comes in transitively with
# its own version constraint. Pinning it separately triggers a pip
# dependency-resolution conflict with supabase 2.8.x.
supabase==2.8.1

# Redis
redis==5.1.1

# Cohere reranker (optional)
cohere==5.11.4

# PDF parsing + images
PyMuPDF==1.24.13
Pillow==10.4.0

# RAG utilities
# (rank-bm25 removed — the retriever is now dense-only)
tiktoken==0.8.0
numpy==1.26.4

# Env + config
python-dotenv==1.0.1

# Logging / utils
structlog==24.4.0
tenacity==9.0.0   # retry with backoff

# Testing (dev)
pytest==8.3.3
pytest-asyncio==0.24.0
